<footer>
  <small>
    <% if current_user %>
    
      <% if current_user.admin == false %>
        Signed in as <%= current_user.firstname %>
      <% else %>
        Signed in as Admin (<%= link_to "Log out", sessions_path("current"), method: :delete %>)<br>
        <%= link_to "Users Management", admin_users_path %>
      <% end %>
    <% end %>
  </small>
</footer>